Checkpoints (thesis write-up)

Purpose
 - When you start writing thesis chapters that describe implementation/results, log a checkpoint here.
 - For each checkpoint: record the command used, the output prefix, and the exact eval artefacts you cite.


2026-01-23 — “Started writing implementation chapter”
 - Recommended citation artefacts (baseline at time of writing):
   - Strict (hard laycan end): `Visualizations/output/eval_thesis_latest_manual_match_physical.txt`
   - Relaxed (lateness penalised): `Visualizations/output/eval_thesis_latest_manual_match_coverage.txt`
 - Command used:
   - `uv run python Models/run_thesis_optimizer_demo.py --time-limit-sec 60 --output-prefix thesis_latest --skip-gantt`
 - Notes:
   - A baseline snapshot of outputs was stored at `Visualizations/output/baseline_2026-01-23_104007/`.

2026-01-23 — “Vessel activation penalty added (toggle)”
 - Code change:
   - Added `vessel_used_penalty` objective term (per activated vessel) in `Models/optimizer_mip.py`.
   - Exposed as CLI knobs in `Models/run_thesis_optimizer_demo.py`:
     - `--vessel-used-penalty-strict`
     - `--vessel-used-penalty-coverage`
 - Example run prefix (strict penalty only):
   - `Visualizations/output/eval_thesis_latest2_strict_vpen_1e5_manual_match_physical.txt`
   - `Visualizations/output/eval_thesis_latest2_strict_vpen_1e5_manual_match_coverage.txt`

2026-01-23 — “Conservative reposition fallback implemented”
 - Code change:
   - `Models/optimizer_mip.py`: missing port-pair distance no longer defaults to 0.0; uses configurable
     `missing_reposition_days` (default 1.0 day), and logs missing-reposition arc counts.
   - `Models/run_thesis_optimizer_demo.py`: new CLI flag `--missing-reposition-days` (default 1.0) passed to both runs.
 - Example run prefixes:
   - `Visualizations/output/eval_thesis_fallback1_manual_match_physical.txt`
   - `Visualizations/output/eval_thesis_fallback1_manual_match_coverage.txt`

2026-01-23 — “Stability lock for thesis runs”
 - Baseline policy (frozen):
   - Use `--missing-reposition-days 1.0` for the main strict baseline.
 - Sensitivity policy:
   - Keep `--missing-reposition-days 0.5` as sensitivity variant only.
 - Stability rule:
   - Do not change fallback defaults/assumptions again unless there is a critical modelling bug.

2026-02-27 — “Port-ID coverage guardrail + export SQL fix”
 - Code/docs changes:
   - `Models/run_thesis_optimizer_demo.py`: added coverage diagnostics and fail-fast gate:
     `--min-dest-port-coverage` (default 0.90), `--require-dest-port-coverage`.
   - `DataSets/Raw/General/sqls_for_optimizer_economics.txt`: voyage export now derives
     ORIGIN/DEST from `TBL_PORT_CALL` + `TBL_REASON_FOR_CALL` (`L`/`D`) with fallback to voyage fields.
 - Validation run:
   - `uv run python Models/run_thesis_optimizer_demo.py --output-prefix thesis_destcovcheck --skip-gantt`
   - Gate test: `uv run python Models/run_thesis_optimizer_demo.py --output-prefix thesis_destcovcheck_gate --skip-gantt --require-dest-port-coverage`
 - Result note:
   - Current local sample CSV still showed `DEST_PORT_ID` coverage 0% in the effective set; gate failed as expected.

2026-02-27 — “New export loaded; manual alignment gate added”
 - Validation:
   - `unallocated_voyages_detailed.csv` refreshed to 2,015 rows with `DEST_PORT_ID` non-null coverage 95.5%.
 - Consistency issue:
   - Zero `VOYAGE_ID` overlap between `fleet_plan_manual.csv` (62 IDs) and the refreshed candidate-voyage file.
   - Thesis demo manual-filtered effective set becomes empty in this state.
 - Code robustness:
   - `Models/run_thesis_optimizer_demo.py` now fails fast with a clear mismatch message when manual overlap is zero.
   - Empty schedule output handling was hardened to avoid pandas empty-CSV parse crashes.

2026-02-27 — “Aligned sample rerun (post export fix)”
 - Data alignment:
   - `fleet_plan_manual.csv`, `fleet_plan_voyages.csv`, and `unallocated_voyages_detailed.csv` now share the same 116 voyage IDs.
   - Candidate-voyage DEST coverage is 96.6%; destination coverage gate passes.
 - Citation artefacts for this aligned checkpoint:
   - `Visualizations/output/eval_thesis_aligned116_manual_match_physical.txt`
   - `Visualizations/output/eval_thesis_aligned116_manual_match_coverage.txt`
 - Command used:
   - `uv run python Models/run_thesis_optimizer_demo.py --output-prefix thesis_aligned116 --skip-gantt --time-limit-sec 60 --require-dest-port-coverage`
